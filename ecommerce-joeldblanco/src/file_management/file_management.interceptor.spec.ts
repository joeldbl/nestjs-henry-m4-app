import { FileManagementInterceptor } from './file_management.interceptor';

describe('FileManagementInterceptor', () => {
  it('should be defined', () => {
    expect(new FileManagementInterceptor()).toBeDefined();
  });
});
